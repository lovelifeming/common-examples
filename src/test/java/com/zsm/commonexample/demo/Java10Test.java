package com.zsm.commonexample.demo;

/**
 *
 * 局部变量类型推断（Local-Variable Type Inference）var是一个保留类型名称，而不是关键字。所以之前使用var作为变量、方法名、包名的都没问题，但是如果作为类或接口名，那么这个类和接口就必须重命名了
 *var的使用场景主要有以下四种：
 *
 * 本地变量初始化。
 * 增强for循环中。
 * 传统for循环中声明的索引变量。
 * Try-with-resources 变量。
 *
 *Optional类添加了新的方法orElseThrow。相比于已经存在的get方法，这个方法更推荐使用
 *
 *GC改进和内存管理 并行全垃圾回收器 G1
 *
 *
 *新增API：ByteArrayOutputStream
 *
 * 新增API：List、Map、Set
 *
 * 新增API：java.util.Properties
 *
 * 新增API： Collectors收集器
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 * @Author: zengsm.
 * @Description:
 * @Date:Created in 2018/12/5.
 * @Modified By:
 */
public class Java10Test
{
}
